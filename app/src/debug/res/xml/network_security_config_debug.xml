<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only (see app/src/debug/AndroidManifest.xml: this file is never merged into a
  release build). Allows plain HTTP only to the Android emulator's alias for the host machine
  (10.0.2.2, where FastAPI runs during local development) and to loopback addresses, for testing
  against a backend run directly on a device via adb reverse. Every other destination still
  requires HTTPS, matching the platform default.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
